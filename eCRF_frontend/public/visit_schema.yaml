classes:
  Visit:
    attributes:
      name:
        range: string
        description: Visit name (e.g. Baseline)
        required: true
      scheduledDate:
        range: date
        description: Scheduled calendar date
        required: false

